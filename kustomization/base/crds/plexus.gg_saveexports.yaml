---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: saveexports.plexus.gg
spec:
  group: plexus.gg
  names:
    kind: SaveExport
    listKind: SaveExportList
    plural: saveexports
    shortNames:
    - se
    singular: saveexport
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              SaveExportSpec is the backend-authorized request for one managed save
              export. No filesystem path is accepted here; the game adapter owns it.
            properties:
              expiresAt:
                format: date-time
                type: string
              gameID:
                minLength: 1
                type: string
              ownerUserID:
                minLength: 1
                type: string
              serverID:
                minLength: 1
                type: string
              setupID:
                minLength: 1
                type: string
              uploadURLSecretRef:
                minLength: 1
                type: string
            required:
            - expiresAt
            - gameID
            - ownerUserID
            - serverID
            - setupID
            - uploadURLSecretRef
            type: object
          status:
            properties:
              archiveBytes:
                format: int64
                minimum: 0
                type: integer
              archiveName:
                type: string
              finishedAt:
                format: date-time
                type: string
              message:
                maxLength: 512
                type: string
              phase:
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Expired
                type: string
              progressPercent:
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              stage:
                description: Stage is a bounded operational stage, not byte-level
                  progress.
                maxLength: 32
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
