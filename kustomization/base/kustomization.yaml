apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # CRDs must come first (generated by make manifests)
  - ./crds
  - ./role.yaml
  - ./crd-reader.yaml

  # Manager Deployment, ServiceAccount, RoleBinding, and ClusterRoleBinding
  # live in overlays/prod so the Dev CRD overlay does not run a manager.
