apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # Add secrets, image overrides, etc. here when ready
  # - ./controller-secret.yaml
  # - ./image-automation.yaml

# Example image override (will be managed by Flux image automation)
# images:
#   - name: controller
#     newName: ghcr.io/anthonyposchen/plexus-controller
#     newTag: latest
